# Lessons: <area>

> Copy this file to `lessons/<area>.md` (e.g. `lessons/build.md`) and add the row to the index in
> `README.md`. Each entry uses this file's own `L-<AREA>-<n>` counter. Delete this line.

## L-<AREA>-1. <blunt title of the mistake>

**What happened.** <Brief in-repo context: what we did and how it bit us. One or two sentences.>

**Root cause.** <The actual reason — the thing that was true that we didn't know.>

**Fix.** <What actually works — the solution, concretely enough to apply again: the command, the pattern,
the setting.>

**Rule.** <The directive that prevents recurrence, so the next agent just follows it.>

---

## L-<AREA>-2. <next lesson>

**What happened.** <...>

**Root cause.** <...>

**Fix.** <...>

**Rule.** <...>
