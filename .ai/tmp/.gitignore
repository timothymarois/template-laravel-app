# .ai/tmp/ — local scratch space for AI-generated temporary docs, assets, and experiment output.
# The folder is kept in git (via .gitkeep); everything you put inside it is ignored and stays local.
# Use it for throwaway work; never for durable knowledge (that goes in a docs/ home).
*
!.gitkeep
!.gitignore
