import { describe, it, expect } from 'vitest';

describe('Example', () => {
    it('works', () => {
        expect(true).toBe(true);
    });

    it('adds numbers correctly', () => {
        expect(1 + 1).toBe(2);
    });
});
