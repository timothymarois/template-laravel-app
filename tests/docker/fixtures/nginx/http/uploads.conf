# Fixture: http-context project configuration.
# Shared-memory zones cannot be declared in a server or location block, which is
# why the template offers an http-context include at all.
limit_req_zone  $binary_remote_addr zone=tpl_upload_req:1m rate=10r/m;
limit_conn_zone $binary_remote_addr zone=tpl_upload_conn:1m;
